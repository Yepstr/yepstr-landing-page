{% set phc = page.header.content %}

<section class="section margin-v-lg">
  <div class="container no-grid">
    <div class="testimonials">
      {% for testimonial in phc.testimonials %}
        <div class="testimonial testimonial--{{ testimonial.type }}">
          <div class="testimonial--bubble padding-sm center-content">
            <div>
              {{ testimonial.quote }}
            </div>
          </div>
          <div class="testimonial--image">
            {{ page.find('/images').media[testimonial.image].html('', '', 'img--circle') }}
          </div>
          <h2 class="testimonial--name">{{ testimonial.name }}</h2>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
