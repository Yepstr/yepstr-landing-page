{% extends 'shared/base.html.twig' %}
{% block content %}

  <div class="section--intro-header section--intro-header--full background background--darkBlue background--pos-center" style="background-image: url({{ base_url }}/user/pages/images/hero-start.jpg) ;">
    <div class="text-wrapper text-wrapper--lg text--white">
      <h1 class="intro-header--headline">{{ page.header.content.intro.headline }}</h1>
      <h2 class="intro-header--sub-headline">{{ page.header.content.intro.sub_headline }}</h2>
      <div class="margin-v-sm">
        <a href="{{ base_url }}/uppdragsgivare/sa-funkar-det" class="btn btn--primary btn--orange btn--inline js-scroll-to-section ga-scroll-to-link" data-target="download-app">Kom igång!</a>
      </div>
    </div>
    <div class="js-scroll-to-section ga-scroll-to-link" data-target="download-app"><img class="intro-splash-image" src="{{ url('theme://build/images/graphics/app-splash.png') }}"></div>
  </div>
  <!--  -->
  {{ include('partials/task_navigation.html.twig') }}
  <!--  -->
  {% for module in page.collection( ) %}
    {{ module.content }}
  {% endfor %}
  <!--  -->
{% endblock %}
